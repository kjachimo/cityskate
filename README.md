# CitySkate

## How to run

### Requirements

* Java (17 or newer)
* Maven

### Run

In project directory run:

```
mvn spring-boot:run
```

Application will start on:

```
http://localhost:8080
```

Swagger (API UI):

```
http://localhost:8080/CitySkate/swagger-ui/index.html
```